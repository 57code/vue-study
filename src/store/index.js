import Vue from 'vue'
import Vuex from 'vuex'

// 想要执行install：
// 1.this.$store
Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
